#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for lst in matrix:
        lstlen = len(lst)
        for i in list:
            if lst[i] < lenlst - 1:
                print("{}".format(lst[i]), end=' ')
            else:
                print("{}".format(lst[i]))
